# HexLang — 全球唯一性声明与技术壁垒

> **核心原则：不拼人力拼定义**  
> 核心数据结构（卦象 Schema、工程语义标签集）的解释权握在自己手中，代码可 MIT，定义不可复制。

---

## 1. 什么是 Hex64？

Hex64 是**全球首个将《易经》六十四卦系统化映射到软件工程语义的符号编码体系**。

它不是占卜工具，不是玄学系统，而是一个**确定性工程语义框架**：

```
输入: "数据库连接池耗尽"
Hex64: 水雷屯 (000011) → [资源耗尽/初创困境] → 建议：扩容 + 限流
```

### 关键区别

| 维度 | 传统易学 | Hex64 |
|------|---------|-------|
| 目的 | 预测未来 | 工程语义分析 |
| 编码 | 人工查表 | DJB2 哈希确定性编码 |
| 输出 | 吉凶判断 | 可执行工程建议 |
| 数据源 | 古籍注释 | Wilhelm 数据集 + 现代工程语义 |
| 可复现 | ❌ 依赖解读者 | ✅ 相同输入 → 相同卦象 |

---

## 2. 技术壁垒（同行抄不走）

### 2.1 邵雍先天序 + 工程语义标签 = 唯一组合

邵雍的先天八卦排序（1629 年提出）是**公共领域知识**，任何人都可以用。  
但将 64 卦按先天序排列后，**逐一映射到软件工程语义标签**（如"承载→存储"、"决断→分支"），这是 HexLang 的原创贡献。

**法律依据：**
- 邵雍先天序 → 公共领域，无版权
- Hex64 工程语义映射 → CC BY-NC 4.0，解释权归属 HexLang 项目

### 2.2 爻级加权特征（Yao-weighted Features）

传统易学只看卦象整体含义。Hex64 引入**爻级权重**，对每卦的 6 条爻分别赋予权重值，形成 6 维特征向量：

```python
hex_result["yao_weights"] = [0.16, 0.36, 0.14, 0.14, 0.18, 0.32]
# 初爻(基础层)  二爻(执行层)  三爻(过渡)  四爻(协调)  五爻(决策)  上爻(输出)
```

这是 HexLang 的**独有计算模式**，在公开文献中无先例。

### 2.3 互卦上下文推演（Inter-hexagram Context）

从本卦的中间 4 爻推导出互卦，提供**过程维度**的分析：

```
本卦: 天水讼 (冲突) → 互卦: 地雷复 (恢复)
解读: 当前冲突，但中间过程指向恢复和循环
```

### 2.4 64 维规则嵌入（Rule-based Embedding）

基于距离衰减的稀疏嵌入，捕捉卦象之间的语义相似度：

```python
# 卦 A 和卦 B 的距离为 1（仅差一爻），cosine similarity = 0.3
# 距离为 2，cosine similarity = 0.1
```

### 2.5 LoRA 微调闭环

通过 feedback.json 积累运维场景修正数据，自动重训 Qwen 模型适配器：

```
用户反馈 → feedback.json → 重新生成训练数据 → LoRA 重训 → 新版本 adapter
```

这是 HexLang 的**自进化能力**，随着使用数据积累，模型会越来越懂运维场景。

---

## 3. 差异化分析（vs 竞品）

### 3.1 vs 其他 AI 工程化工具

| 工具 | 能力 | Hex64 优势 |
|------|------|-----------|
| LangChain | 通用 RAG 框架 | Hex64 提供**确定性的语义结构** |
| OpenAI Functions | JSON schema 约束 | Hex64 有**64 卦完整语义体系** |
| Prometheus Alertmanager | 告警路由 | Hex64 提供**卦象推理链** |

### 3.2 vs 其他 I Ching 数字化工具

| 工具 | 能力 | Hex64 优势 |
|------|------|-----------|
| 各种手机易经 App | 占卜/查询 | Hex64 是**工程语义系统**，非玄学 |
| I Ching API (npm) | 基础卦象查询 | Hex64 有**爻权重 + 互卦 + 嵌入** |
| Python I Ching 库 | 随机起卦 | Hex64 是**确定性编码**，非随机 |

### 3.3 唯一性总结

HexLang 在以下三个维度的组合是**全球唯一**的：

1. **邵雍先天序**（历史公共知识）
2. **工程语义标签映射**（HexLang 原创，CC BY-NC 4.0）
3. **LoRA 微调自进化**（HexLang 实现，MIT License）

---

## 4. 防御策略

### 4.1 法律层面

- **代码**: MIT License — 允许自由使用，但必须保留版权头
- **语义定义**: CC BY-NC 4.0 — 署名、非商用
- **商标**: 计划 2027.8 申请 HexLang 文字 + Logo 商标
- **DMCA**: 发现 Fork 换皮商用立即投诉

### 4.2 技术层面

- **确定性编码**: DJB2 哈希算法公开但编码规则（bin→语义映射）受保护
- **Adapter 商店**: 预训适配器（ops/fintech/lowcode）形成生态壁垒
- **社区粘性**: 每月迭代，保持版本领先

### 4.3 时间层面

- **先发优势**: 2026.7 完成核心闭环，全球首个 Hex64 × Engineering 系统
- **数据飞轮**: 反馈数据越多 → 模型越准 → 用户越多 → 数据越多
- **学术背书**: 计划 2027.10 发表技术白皮书

---

## 5. 时间线证据链

| 日期 | 事件 | 证据 |
|------|------|------|
| 2026-07-08 | Hex64 代码审计与修缮 | Git commit history |
| 2026-07-09 | Python Encoder 架构升级 | encoder.py 版本记录 |
| 2026-07-09 | LoRA 微调训练完成 | adapters/hex64-v1/ |
| 2026-07-09 | Feedback 闭环管理上线 | feedback_manager.py |
| 2026-07-09 | 合规防护落地 | LICENSE + _sources.json |

---

## 6. 引用与数据来源

Hex64 数据整合了以下开源资源：

| 来源 | 内容 | 许可 |
|------|------|------|
| [adamblvck/iching-wilhelm-dataset](https://github.com/adamblvck/iching-wilhelm-dataset) | Unicode 符号 + 英文名 + 威廉注释 | CC BY-NC 4.0 |
| [qntm/hexagram-encode](https://qntm.org/hex/) | Base64 ↔ 卦符映射 | Public Domain |
| [chengjun/iching](https://github.com/chengjun/iching) | 中文原文/白话文解释 | MIT |

完整数据溯源见 [data/_sources.json](data/_sources.json)。

---

*本文档最后更新: 2026-07-09*  
*维护者: zjwjing / HexLang project*
